apply plugin: 'java'
apply plugin: 'kotlin'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile deps.kotlin
    compile deps.rxKotlin

    compile deps.testJUnit

    testCompile deps.testJUnit
    testCompile deps.testMockito
    testCompile deps.testHamcrest
}
